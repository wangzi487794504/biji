#### 规则引擎

* 规则引擎就是提供一种可选的计算模型。与通常的命令式模型（由带有条件和循环的命令依次组成）不同，规则引擎基于生产规则系统。这是一组生产规则，每条规则都有**一个条件（condition）和一个动作（action）**———— 简单地说，可以将其看作是一组if-then语句。

* 规则可以按任何顺序编写，引擎会决定何时使用对顺序有意义的任何方式来计算它们。考虑它的一个好方法是系统运行所有规则，选择条件成立的规则，然后执行相应的操作。这样做的好处是，很多问题都很自然地符合这个模型

* Easy Rules它提供Rule抽象以创建具有条件和动作的规则，并提供RuleEngine API，该API通过一组规则运行以评估条件并执行动作。 

* maven

  ```xml
  <!-- https://mvnrepository.com/artifact/org.jeasy/easy-rules-core -->
  <dependency>
      <groupId>org.jeasy</groupId>
      <artifactId>easy-rules-core</artifactId>
      <version>4.1.0</version>
  </dependency>
  
  ```

  

* 规则引擎入门

  * 方式一

    ```java
    @Rule(name = "weather rule", description = "if it rains then take an umbrella")
    public class WeatherRule {
    
        @Condition
        public boolean itRains(@Fact("rain") boolean rain) {
            return rain;
        }
        
        @Action
        public void takeAnUmbrella() {
            System.out.println("It rains, take an umbrella!");
        }
    }
    ```

  * 链式编程

    ```java
    Rule weatherRule = new RuleBuilder()
            .name("weather rule")
            .description("if it rains then take an umbrella")
            .when(facts -> facts.get("rain").equals(true))
            .then(facts -> System.out.println("It rains, take an umbrella!"))
            .build();
    ```

  * 表达式

    ```java
    Rule weatherRule = new MVELRule()
            .name("weather rule")
            .description("if it rains then take an umbrella")
            .when("rain == true")
            .then("System.out.println(\"It rains, take an umbrella!\");");
    ```

  * yml配置

    ```yml
    name: "weather rule"
    description: "if it rains then take an umbrella"
    condition: "rain == true"
    actions:
      - "System.out.println(\"It rains, take an umbrella!\");"
    ```

    

* 实操

  * 编写第一个规则

    ```java
    public class Main {
        public static void main(String[] args) {
            //它里面是一个hashset
            Facts facts=new Facts();
            //把我们自己的加入进入
            facts.put("rain",true);
    
            //创建规则
            Rules rules=new Rules();
            rules.register(new RuleFirst());
            //创建规则引擎
            RulesEngine rulesEngine=new DefaultRulesEngine();
            rulesEngine.fire(rules,facts);
        }
    }
    ```

  * 规则

    ```java
    @Rule(name = "helloRule",description = "第一个rule")
    public class RuleFirst {
        @Condition
        public boolean conditionFirst(@Fact("rain") Boolean rain){
             return rain;
        }
        @Action
        public void takeAnUmbrella() {
            System.out.println("hello rules");
        }
    }
    ```

    

  

* 定义规则

  * 规则包括：

    * *名称*：规则命名空间中的唯一规则名称
    * *Description*：规则的简要描述
    * *Priority*：相对于其他规则的规则优先级
    * *事实*：触发规则时的一组已知事实
    * *条件*：在给定某些事实的情况下应满足的一组条件，以便应用规则
    * *操作*：满足条件时要执行的操作集（可以添加/删除/修改事实）

  * 定义了一个规则接口

    ```java
    public interface Rule extends Comparable<Rule> {
    
        /**
        * This method encapsulates the rule's conditions.
        * @return true if the rule should be applied given the provided facts, false otherwise
        */
        boolean evaluate(Facts facts);
    
        /**
        * This method encapsulates the rule's actions.
        * @throws Exception if an error occurs during actions performing
        */
        void execute(Facts facts) throws Exception;
    
        //Getters and setters for rule name, description and priority omitted.
    
    }
    ```

  * 该方法封装了必须计算为 *TRUE* 才能触发规则的条件。 该方法封装了在满足规则条件时应执行的操作。 条件和操作由 和 接口表示。`evaluate``execute``Condition``Action`

  * 规则

    * 通过在 POJO 上添加注释来声明

      ```java
      @Rule(name = "my rule", description = "my rule description", priority = 1)
      public class MyRule {
      
          @Condition
          public boolean when(@Fact("fact") fact) {
              //my rule conditions
              return true;
          }
      
          @Action(order = 1)
          public void then(Facts facts) throws Exception {
              //my actions
          }
      
          @Action(order = 2)
          public void finally() throws Exception {
              //my final actions
          }
      
      }
      ```

      * 注释标记要执行以评估规则条件的方法。 此方法必须是 public，可以有一个或多个带 Comments 的参数，并返回布尔类型。只能使用注释对一种方法进行注释。`@Condition``@Fact``@Condition`

        注释标记要执行以执行规则操作的方法。规则可以有多个操作。可以使用 *order* 属性按指定顺序执行操作。默认情况下，操作的顺序为 0。`@Action`

    * 通过 API 以编程方式`RuleBuilder`

    * 这些是定义规则的最常见方法，但如果需要，您也可以实现接口或扩展类。`Rule``BasicRule`

      ```java
      Rule rule = new RuleBuilder()
                      .name("myRule")
                      .description("myRuleDescription")
                      .priority(3)
                      .when(condition)
                      .then(action1)
                      .then(action2)
                      .build();
      ```

    

    

* 复合规则

  * Easy Rules 允许您从原始规则创建复杂的规则。A 由一组规则组成。这通常是[复合设计模式](http://en.wikipedia.org/wiki/Composite_pattern)的实现。`CompositeRule`

    复合规则是一个抽象概念，因为组合规则可以以不同的方式触发。Easy Rules 附带了 3 种实现，可以在模块中找到：`CompositeRule``easy-rules-support`

    - `UnitRuleGroup`：单元规则组是充当一个单元的复合规则：要么应用所有规则，要么不应用任何规则。
    - `ActivationRuleGroup`：激活规则组是一种复合规则，它触发第一个适用的规则并忽略组中的其他规则（XOR 逻辑）。规则首先按其在组内的自然顺序（默认为优先级）进行排序。
    - `ConditionalRuleGroup`：条件规则组是一种复合规则，其中优先级最高的规则用作条件：如果优先级最高的规则的计算结果为 true，则触发其余规则。

  

  

* 定义事实

  * 先随便写一个需要判定的事实

    ```java
    public class Fact<T> {
       private final String name;
       private final T value;
    }
    ```

    * 事实具有名称和值，两者都**不能**是 。另一方面，API 表示一组事实，并充当事实的命名空间。这意味着事实在实例中必须具有唯一的名称。`null``Facts``Facts`

  * 事实实例

    ```java
    Fact<String> fact = new Fact("foo", "bar");
    Facts facts = new Facts();
    facts.add(fact);
    ```

    

  

* 实战2，使用 Easy Rules 实现 FizzBuzz 应用程序。 FizzBuzz 是一个简单的应用程序，需要从 1 到 100 计数：

  * 源代码

    ```java
    public class FizzBuzz {
      public static void main(String[] args) {
        for(int i = 1; i <= 100; i++) {
          if (((i % 5) == 0) && ((i % 7) == 0))
            System.out.print("fizzbuzz");
          else if ((i % 5) == 0) System.out.print("fizz");
          else if ((i % 7) == 0) System.out.print("buzz");
          else System.out.print(i);
          System.out.println();
        }
        System.out.println();
      }
    }
    ```

    

  * 如果数字是 5 的倍数，则打印 “fizz”

    ```java
    @Rule
    public class FizzRule {
        @Condition
        public boolean isFizz(@Fact("number") Integer number) {
            return number % 5 == 0;
        }
        @Action
        public void printFizz() {
            System.out.print("fizz");
        }
        @Priority
        public int getPriority() {
            return 1;
        }
    }
    ```

    

  * 如果数字是 7 的倍数，则打印 “buzz”

    ```java
    @Rule
    public class BuzzRule {
        @Condition
        public boolean isBuzz(@Fact("number") Integer number) {
            return number % 7 == 0;
        }
        @Action
        public void printBuzz() {
            System.out.print("buzz");
        }
        @Priority
        public int getPriority() {
            return 2;
        }
    }
    ```

    

  * 如果数字是 5 和 7 的倍数，则打印 “fizzbuzz”，把上面两个类传进来

    ```java
    public class FizzBuzzRule extends UnitRuleGroup {
    
        public FizzBuzzRule(Object... rules) {
            for (Object rule : rules) {
                addRule(rule);
            }
        }
    
        @Override
        public int getPriority() {
            return 0;
        }
    }
    ```

    

  * 否则打印数字本身

    ```java
    @Rule
    public class NonFizzBuzzRule {
        @Condition
        public boolean isNotFizzNorBuzz(@Fact("number") Integer number) {
            // can return true, because this is the latest rule to trigger according to assigned priorities
            // and in which case, the number is not fizz nor buzz
            return number % 5 != 0 || number % 7 != 0;
        }
        @Action
        public void printInput(@Fact("number") Integer number) {
            System.out.print(number);
        }
        @Priority
        public int getPriority() {
            return 3;
        }
    }
    ```

  * 测试

    ```java
    public class FizzBuzzWithEasyRules {
        public static void main(String[] args) {
            // create a rules engine
            RulesEngineParameters parameters = new RulesEngineParameters().skipOnFirstAppliedRule(true);
            RulesEngine fizzBuzzEngine = new DefaultRulesEngine(parameters);
            // create rules
            Rules rules = new Rules();
            rules.register(new FizzRule());
            rules.register(new BuzzRule());
            rules.register(new FizzBuzzRule(new FizzRule(), new BuzzRule()));
            rules.register(new NonFizzBuzzRule());
            // fire rules
            Facts facts = new Facts();
            for (int i = 1; i <= 100; i++) {
                facts.put("number", i);
                fizzBuzzEngine.fire(rules, facts);
                System.out.println();
            }
        }
    }
    ```

    

  

  

