###### 直接插入排序

* 遍历，与之前的比较，比他小就逐个比较换位

  ```java
  public class InsertSort {
      static int[]  sort(int[] nums){
          //先让局部有序
          int j;
          //0用来当哨兵
          for (int i = 1; i < nums.length; i++) {
              //todo 后一个比前一个小
              if (nums[i]<nums[i-1]){
                 int temp=nums[i];//复制为哨兵
                  //j>=0必须放在前面，负责nums[j]的j会取到-1
                  for (j = i-1; j>=0 &&temp<nums[j]; j--) {
                      nums[j+1]=nums[j];
                  }
                  nums[j+1]=temp;
              }
          }
          return nums;
      }
  
      public static void main(String[] args) {
          int[] nums=new int[]{49,38,65,97,76,13,27,49,55,4};
          sort(nums);
          for (int num : nums) {
              System.out.println(num);
          }
      }
  }
  ```



###### 折半插入

* 在简单插入的基础上有序队列部分采用了二分法



###### 希尔排序

* 分段排序，每一段里面采用直接插入排序，段长开始为数组的一半，最后为1，即最后来一个全局的简单排序

###### 冒泡排序

* 每一次都去找最小的那一个

###### 快速排序

* 选择一个基数，用双指针交换，然后再排两个子序列，不断递归

###### 简单排序

* 每次就选最小的



###### 计数排序

* 属于桶排序。有限的取值范围，用help数组表示每个取值，然后遍历原数组