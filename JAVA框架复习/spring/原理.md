![image-20241012152122282](%E5%8E%9F%E7%90%86.assets/image-20241012152122282.png)

![image-20241012152206738](%E5%8E%9F%E7%90%86.assets/image-20241012152206738.png)

* 第一步：实例化：是在堆内存中申请空间，此时对象的属性值一般是默认值。就是反射创建对象的过程

  * 在doCreate()方法里，有一个createBeanInstance()，他是去实例化的。

    ```JAVA
    public abstract class AbstractAutowireCapableBeanFactory extends AbstractBeanFactory
    		implements AutowireCapableBeanFactory{
        
        protected BeanWrapper createBeanInstance(String beanName, RootBeanDefinition mbd, @Nullable Object[] args) {
    		// Make sure bean class is actually resolved at this point.
            //先获取到class对象
    		Class<?> beanClass = resolveBeanClass(mbd, beanName);
    
    		if (beanClass != null && !Modifier.isPublic(beanClass.getModifiers()) && !mbd.isNonPublicAccessAllowed()) {
    			throw new BeanCreationException(mbd.getResourceDescription(), beanName,
    					"Bean class isn't public, and non-public access not allowed: " + beanClass.getName());
    		}
    
    		Supplier<?> instanceSupplier = mbd.getInstanceSupplier();
    		if (instanceSupplier != null) {
    			return obtainFromSupplier(instanceSupplier, beanName);
    		}
    
    		if (mbd.getFactoryMethodName() != null) {
    			return instantiateUsingFactoryMethod(beanName, mbd, args);
    		}
    
    		// Shortcut when re-creating the same bean...
    		boolean resolved = false;
    		boolean autowireNecessary = false;
    		if (args == null) {
    			synchronized (mbd.constructorArgumentLock) {
    				if (mbd.resolvedConstructorOrFactoryMethod != null) {
    					resolved = true;
    					autowireNecessary = mbd.constructorArgumentsResolved;
    				}
    			}
    		}
    		if (resolved) {
    			if (autowireNecessary) {
    				return autowireConstructor(beanName, mbd, null, null);
    			}
    			else {
    				return instantiateBean(beanName, mbd);
    			}
    		}
    
    		// Candidate constructors for autowiring?
            //获取构造器
            //根据策略模式选择实例化策略
    		Constructor<?>[] ctors = determineConstructorsFromBeanPostProcessors(beanClass, beanName);
    		if (ctors != null || mbd.getResolvedAutowireMode() == AUTOWIRE_CONSTRUCTOR ||
    				mbd.hasConstructorArgumentValues() || !ObjectUtils.isEmpty(args)) {
    			return autowireConstructor(beanName, mbd, ctors, args);
    		}
    
    		// Preferred constructors for default construction?
    		ctors = mbd.getPreferredConstructors();
    		if (ctors != null) {
    			return autowireConstructor(beanName, mbd, ctors, null);
    		}
    
    		// No special handling: simply use no-arg constructor.
    		return instantiateBean(beanName, mbd);
    	}
    }
    ```

  * 第二步：自定义的赋值，populateBeanset方法完成赋值操作

  * 第三步：invokeAwareMethods 检查aware相关接口 ，给容器对象属性赋值。 Aware是一个什么都没有的接口。需要给出一个标识表明操作的时机。让你自定义的bean能够调用容器的bean。就是看我们自己的bean有没有继承这些接口，有这些接口就给他注入

  *  理论上执行到这里就可以用了，但是spring要考虑扩展性。考虑是不是需要代理

  * 第四步：执行前置处理方法 ,执行后置处理方法。它是BeanPostProcessor，里面只有两个方法postProcessBeforeInitialization，一个是postProcessAfterInitialization：**归根结底它们是做扩展的实现**

    * 在这里实现动态代理：一个是jdk的，一个是cglib

    * 后置代码做了处理，前缀直接return bean

      ```java
      	public Object postProcessAfterInitialization(@Nullable Object bean, String beanName) {
      		if (bean != null) {
      			Object cacheKey = getCacheKey(bean.getClass(), beanName);
      			if (this.earlyProxyReferences.remove(cacheKey) != bean) {
      				return wrapIfNecessary(bean, beanName, cacheKey);
      			}
      		}
      		return bean;
      	}
      ```

    * wrapIfNecessary是不是需要代理，点进去

      ```java
      	protected Object wrapIfNecessary(Object bean, String beanName, Object cacheKey) {
      		if (StringUtils.hasLength(beanName) && this.targetSourcedBeans.contains(beanName)) {
      			return bean;
      		}
      		if (Boolean.FALSE.equals(this.advisedBeans.get(cacheKey))) {
      			return bean;
      		}
      		if (isInfrastructureClass(bean.getClass()) || shouldSkip(bean.getClass(), beanName)) {
      			this.advisedBeans.put(cacheKey, Boolean.FALSE);
      			return bean;
      		}
      
      		// Create proxy if we have advice.
      		Object[] specificInterceptors = getAdvicesAndAdvisorsForBean(bean.getClass(), beanName, null);
      		if (specificInterceptors != DO_NOT_PROXY) {
      			this.advisedBeans.put(cacheKey, Boolean.TRUE);
      			Object proxy = createProxy(
      					bean.getClass(), beanName, specificInterceptors, new SingletonTargetSource(bean));
      			this.proxyTypes.put(cacheKey, proxy.getClass());
      			return proxy;
      		}
      
      		this.advisedBeans.put(cacheKey, Boolean.FALSE);
      		return bean;
      	}
      ```

    * `当看到这里你就知道一个问题，AOP是IOC的一个扩展点`

  * 在第四步的before和after的中间，还有一个初始化，叫invokeInitMethods，他会检测bean是否实现了InitializingBean接口。如果实现了他会调用afterPropertiesSet方法。在这里可以自己随意写自己的代码，如属性赋值。

  * 第五步：使用bean对象

  * 第六步：销毁bean对象